# This file is intentionally empty.
#
# Settings for the extension (relay URL + x-api-key) are now configured
# via the in-extension UI (toolbar icon → Settings) and persisted in
# chrome.storage.local — they are NOT read from .env anymore.
